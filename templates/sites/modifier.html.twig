{% include 'base.html.twig' %}
<div>
    <h1>Modification d'un Site : </h1>

    Numéro du site: {{ site.noSite }}<br>
    Nom du site :{{ site.nomSite }}<br><br>


    {{ form_start(siteForm) }}
    {{ form_widget(siteForm) }}
    <button type="submit">Send</button>

    {{ form_end(siteForm) }}

    <td><a href="{{ path('sitessites') }}"> <h1>Revenir à la liste des sites</h1> </a></td>
</div>